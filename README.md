# authn-ssr-todos

## Getting Started

Install the dependencies with `pnpm`:

```shell
$ pnpm install
```

Run the `prisma` script to set up the database:

```shell
$ pnpm run prisma
```

Run the dev server:

```shell
$ pnpm run dev
```
